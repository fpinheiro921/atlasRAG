{
  "version": 2,
  "buildCommand": "cd specforge && npm install && npm run build",
  "outputDirectory": "specforge/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}